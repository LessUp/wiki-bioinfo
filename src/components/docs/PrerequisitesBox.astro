---
interface Props {
  title?: string;
  items: string[];
}

const { title = '前置知识', items } = Astro.props;
---

<aside class="note-card">
  <strong>{title}</strong>
  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</aside>
